extends ../layout
block content 
  h2.center  Tableau des Utilisateurs :
  table.table 
    thead
      tr 
        th Name
        th Email 
        th Code Postal 
        th Edition
        th Suppression
    tbody
      each user in users 
        tr 
          td 
            a(href=`/users/${user._id}`)=user.fullName 
          td=user.email 
          td=user.zipCode 
          td 
            a(href=`/users/${user._id}/edit`) Edition 
          td 
            a(href=`/users/${user._id}/delete?_method=DELETE` onclick="return confirm('Êtes-vous sure de vouloir supprimer?')") Suppression